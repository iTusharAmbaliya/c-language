#include<stdio.h>
main()
{
	int a[i] b[i],i,n;
	
	for(i=0; i<n; i++)
	{
		printf("enter a[%d] : ",i);
		scanf("%d",&a[i]);
		b[i] =a[i]*a[i];
	}
		
	for(i=0; i<n; i++)
	{
		printf("a[%d] : +b[%d]n",i,a[i]);
	}
	
}